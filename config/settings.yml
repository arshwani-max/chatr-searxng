general:
  debug: false
  instance_name: "Chatr Search"
  enable_metrics: false

server:
  port: 8080
  bind_address: "0.0.0.0"

search:
  default_lang: "en"
  safe_search: 0
  autocomplete: "duckduckgo"

ui:
  theme: simple
  query_in_title: true

engines:
  - name: duckduckgo
    engine: duckduckgo
    shortcut: ddg
    disabled: false

  - name: google
    engine: google
    disabled: true

  - name: bing
    engine: bing
    disabled: true
